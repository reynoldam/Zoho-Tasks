#include <stdio.h>

int sum(int);

int main()
{
    int numb;

    printf("Enter a number :");
    scanf("%d", &numb);

    printf("%d",sum(numb));

    return 0;
}

int sum(int n)
{
    if (n == 0)
        return 0;
    else
        return n + sum(n - 1);
}